// Auto-generated Playwright test stub for RM-03 — Join invalid code
const { test, expect } = require('@playwright/test');

test.describe('3-2-room-management - RM-03', () => {
  test('RM-03: Join invalid code', async ({ page }) => {
    // Preconditions: (fill as needed)
    // Description: Join invalid code
    // Steps:
    // 1. (step 1)
    // 2. (step 2)
    // Expected: (fill expected result)

    // Example navigation: await page.goto('/');
    await test.skip(); // remove this once test is implemented
  });
});
